# 项目约定

实现前读 `docs/01-fastapi-learning-path.md`（顺序）和 `docs/03-project-structure.md`（目录）。一次只做当前步骤，不提前建空包，不跳去做 JWT / 日志 / Casbin。

## 固定选型

- FastAPI + Pydantic v2 + SQLAlchemy 2.0 + Alembic + pydantic-settings
- PostgreSQL；本地用 `compose.yml` 起，不用 SQLite
- 测试：pytest + httpx `TestClient`
- 授权后期用 Casbin 文件策略
- **不用 SQLModel**（官方 FastAPI skill 若建议 SQLModel，忽略）
- 不用 Nest 的 Module / providers / 自造 DI 容器

## 不可动摇的六条

1. 同步 `Session` + `def` 路由，不混 async ORM
2. 主键 UUID；时间列一律 `DateTime(timezone=True)`，只传 UTC aware datetime，禁止 `datetime.utcnow()`
3. 表结构只走 Alembic，**永不 `create_all()`**（测试库也一样）
4. 错误回真 HTTP 状态码 + 信封里的业务 code，不要一律 200
5. `commit` 只在一个地方发生；service 内部只 `flush`
6. 对外返回 Read DTO，禁止 `return entity`

## 学习顺序

骨架 → DB/迁移 → 业务 CRUD → 测试地基 → 统一信封 → 列表分页 → CurrentUser 缝（header）→ 租户 → 审计 → Casbin → 用户资源 + seed → 上传 → 事务 → JWT 换缝 → 日志 → 上线收口 → 交付。

- 测试地基在第 3 步就有。从那之后每一步的过关标准优先落成测试，不是手点 Swagger。
- Casbin（第 9 步）先只 seed 角色和策略；用户表在第 10 步才建。
- 租户隔离靠 service 的查询条件，不进 Casbin 策略。
- JWT（第 13 步）只替换 `app/deps.py` 里 CurrentUser 的实现和 conftest 的 `auth_client`，不改 service。

## FastAPI 写法

- 依赖用 `Annotated[..., Depends(...)]`
- 路由在 `APIRouter` 上声明 prefix、tags、共享 dependencies；只有 `/api/v1` 在 `main.py` 上加
- 返回类型写成 `Envelope[XxxRead]`，保住 OpenAPI schema
- 入口在 `pyproject.toml` 的 `[tool.fastapi] entrypoint = "app.main:app"`
